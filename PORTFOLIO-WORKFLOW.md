# 포트폴리오 제작 워크플로우 (마스터 프로세스)

레쥬메/CV·사진·글을 받아 → 인터뷰로 사람을 이해하고 → 자료를 고도화·확정한 뒤 →
디자인 컨셉을 맞추고 → 초안 2안 중 선택 → GitHub 계정에 게시하는 **엔드투엔드 프로세스**.

`drmoony.github.io`가 이 프로세스의 레퍼런스 구현이다.

> 운영 원칙: **품질 > 속도, 범위 좁혀 깊게.** 큰 단계는 phase로 나눠 진행하고,
> 사용자 **컨펌 게이트(◆)**를 통과하지 않으면 다음 단계로 넘어가지 않는다.
> 산출물은 EN/KO 이중언어, 워딩은 **원문 충실 우선(과윤문 금지)**.

---

## 전체 흐름

```
0. 자료 수령(기존)  ─▶  1. 인터뷰  ─▶  2. CV 고도화 + 위키 ◆컨펌
                                                    │
                                                    ▼
   4. 디자인 컨셉 ◆확인  ◀─  3. CV 확정 + 워딩 교정
        │
        ▼
   5. 초안 2안 제작  ─▶  ◆선택  ─▶  6. 게시용 HTML 생성 + 세부 조정(윤문)
                                                    │
                                                    ▼
                                        7. GitHub 게시 ─▶ ◆확정
```

`◆` = 사용자 컨펌 없이는 다음으로 못 넘어가는 게이트.

---

## 0. 자료 수령  *(기존 · 시작점)*

- **받는 것**: 기존 레쥬메/CV, 사진, 그 외 필요한 글(수상 내역, 발표/논문, 프로젝트 설명, 자기소개 등)
- **목적**: 손에 있는 원재료를 빠짐없이 확보. 부족분은 인터뷰에서 채운다.
- **산출물**: 원본 자료 아카이브(무손실 보관 — 사진은 원본 해상도 유지).

## 1. 인터뷰

- **목적**: 이력서에 안 드러나는 맥락을 캔다 — 각 경력의 *실제로 한 일*, 판단의 근거,
  성과의 뒷이야기, 강조하고 싶은 정체성, 피하고 싶은 프레임.
- **방식**: 경력 타임라인을 따라가며 "무엇을/왜/그래서 어떻게 됐나"를 파고든다.
  자료의 빈칸(누락 연도·설명 없는 수상 등)을 질문 리스트로 만들어 메운다.
- **산출물**: 인터뷰 노트(경력별 read/approach/payoff, 인용할 만한 발언, 강조·금지 항목).

## 2. CV 고도화 + 위키 생성  ─ ◆컨펌

- **CV 고도화**: 인터뷰로 얻은 맥락을 반영해 각 경력을 *결과 중심*으로 재작성.
  나열이 아니라 "문제 → 판단 → 성과" 구조로.
- **위키(지식 보관소) 생성**: 인물·경력·프로젝트를 구조화한 **비공개** 내부 지식베이스.
  이후 모든 카피의 단일 출처(SSOT). 포트폴리오 본문·CV·소개글이 여기서 파생되도록 한다.
  → 구현: **`kb/`** 로컬 웹앱 (카테고리 + 항목 CRUD). 실행 `python3 kb/server.py`. 상세 [kb/README.md](kb/README.md).
- **◆컨펌**: 사실관계(수치·연도·기관명·인용)와 강조 방향을 사용자가 검수·승인.
  **사실 오류는 여기서 잡는다** — 이후 단계는 문체만 손댄다.

## 3. CV/레쥬메 확정 + 워딩 교정

- **확정**: 컨펌된 내용으로 CV/레쥬메 최종본 고정(버전 태그).
- **워딩 교정**: 문법·표기·조사·콩글리시·번역투 교정.
  - 한글은 **원문 충실 우선** — 의미·수치·고유명사·인용 100% 보존, 과윤문 금지.
  - AI 티(번역투·영어 인용 과다·기계적 병렬 등)는 `im-not-ai`(`/humanize-korean`)로 제거.
    단순 맞춤법·오탈자는 직접 교정.
- **산출물**: 확정 CV/레쥬메(EN/KO), 교정 로그.

## 4. 포트폴리오 디자인 컨셉 확인  ─ ◆확인

게시 전에 방향을 못박는다. 최소 다음을 사용자와 합의:

| 항목 | 결정할 것 |
|------|-----------|
| **색상** | 팔레트·톤(다크/라이트), 강조색 |
| **구조** | 스크롤형(원페이지) vs 페이지 분할 |
| **느낌** | 미니멀/에디토리얼/테크 등 무드, 참고 레퍼런스 |
| **강조점** | 가장 먼저 각인시킬 것(정체성 한 줄), 섹션 우선순위 |
| **언어** | EN/KO 이중언어 토글 여부, 기본 언어 |

- **◆확인**: 위 다섯을 사용자가 승인해야 초안 제작 착수.

## 5. 초안 2안 제작  ─ ◆선택

- 컨펌된 컨셉 안에서 **뚜렷이 다른 2안**을 만든다(예: 톤/레이아웃/강조 방식 차이).
  한 안을 반복 수정하는 것보다, 서로 다른 방향을 병렬로 보여주면 선택이 빠르고 정확하다.
- **◆선택**: 사용자가 1안을 고르거나, 두 안의 장점을 조합해 방향 확정.

## 6. 게시용 HTML 생성 + 세부 조정  *(텍스트 윤문 포함)*

- 선택안을 GitHub Pages 게시용 단일 `index.html`로 완성(자체 완결 — 외부 의존 최소).
- **세부 조정**: 반응형(데스크톱/모바일), 접근성, 인터랙션, 이미지 최적화.
- **텍스트 윤문(벌크 편집)**: 화면의 모든 EN/KO 문안을 한 파일로 뽑아 일괄 교정 →
  바뀐 텍스트만 정확히 되돌려 반영. → **`text-tools/WORKFLOW.md`** 참조.
  - `extract.py`로 편집 시트 생성 → 사람이 글자만 수정 → `apply.py`로 오프셋 splice 반영.
  - 마퀴 `aria-hidden` 사본까지 동기화, 태그·엔티티 보존, 바뀐 항목만 감지.

## 7. GitHub 게시 + 확정  ─ ◆확정

- 사용자 GitHub 계정(`<user>.github.io`)에 커밋·푸시. **커밋/푸시는 사용자 승인 후에만.**
- 실제 게시 URL에서 최종 확인(데스크톱·모바일, 언어 토글, 링크·이미지).
- **◆확정**: 사용자가 라이브 사이트를 보고 최종 승인 → 완료.

---

## 이 프로세스의 규칙

1. **컨펌 게이트를 건너뛰지 않는다.** 사실관계(2)·컨셉(4)·초안 선택(5)·게시(7)는 사용자 승인 필수.
2. **사실은 앞에서, 문체는 뒤에서.** 2단계에서 사실을 확정하고, 이후엔 워딩만 만진다.
3. **원문 충실 우선.** 윤문은 의미·수치·고유명사·인용을 보존하고 과윤문(변경률 과다)을 피한다.
4. **위키가 단일 출처.** 모든 카피는 위키에서 파생 — CV·포트폴리오·소개글이 어긋나지 않게.
5. **되돌릴 수 있게.** 각 확정본은 버전으로 고정, 게시 텍스트 수정은 `text-tools` 파이프라인으로.
6. **비용 게이트.** 과금 여지가 있는 작업은 예상비용 → 승인 → 진행(자동 실행 금지).

---

## 산출물 지도

| 단계 | 산출물 |
|------|--------|
| 0 | 원본 자료 아카이브 |
| 1 | 인터뷰 노트 |
| 2 | 고도화 CV 초안 + 위키(SSOT) · ◆컨펌 |
| 3 | 확정 CV/레쥬메(EN/KO) + 교정 로그 |
| 4 | 디자인 컨셉 합의서(색상·구조·느낌·강조·언어) · ◆확인 |
| 5 | 초안 2안 · ◆선택 |
| 6 | 게시용 `index.html` + 윤문 반영 (`text-tools/`) |
| 7 | 라이브 `<user>.github.io` · ◆확정 |

---

## 관련 문서

- `kb/README.md` — 2단계 지식 보관소(위키) 로컬 웹앱 사용법
- `text-tools/WORKFLOW.md` — 6단계의 텍스트 벌크 편집 파이프라인 상세
- `im-not-ai` (`/humanize-korean`) — 3·6단계 한글 AI 티 제거 윤문 스킬
